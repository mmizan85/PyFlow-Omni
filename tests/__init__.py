"""Test suite for PyFlow Omni.

Run with: pytest
Tests marked `network` need real internet egress (not just package registries)
and are skipped automatically if that's unavailable — see conftest.py.
"""
